# 推荐用如下命令一键安装依赖（适用于2080Ti，CUDA 11.1）：
# pip install -r requirements.txt -f https://download.pytorch.org/whl/torch_stable.html
#
# 如果你的CUDA版本不同，可参考 https://pytorch.org/get-started/previous-versions/ 选择合适的torch版本

torch==1.10.0+cu113
numpy==1.24.3
tqdm==4.66.1
